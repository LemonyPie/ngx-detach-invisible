import { DetachInvisibleDirective } from './detach-invisible.directive';

describe('DetachInvisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new DetachInvisibleDirective();
    expect(directive).toBeTruthy();
  });
});
